
#include <stdio.h> // only to include if we are using any standard library functions in our custom functions
void changefile(){
    printf("The compiler now executes the content of changed file\n");
}