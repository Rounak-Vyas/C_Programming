// = , += (means perform addition first then assign)
/* a+=5 which is equivalent to a = a+5 
*/

// A program question 
#include <stdio.h>
int main (){
    int a = 7;
    a^=5;   // a = a^5 and store the value in a 
    printf("%d",printf("%d",a+=3));
    return 0;
}